name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'macos-14'
            args: '--target universal-apple-darwin'
            label: 'mac'
          - platform: 'windows-latest'
            args: ''
            label: 'win'

    runs-on: ${{ matrix.platform }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-14' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Cache Cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            src-tauri/target
          key: ${{ matrix.platform }}-cargo-${{ hashFiles('src-tauri/Cargo.lock') }}

      - name: Install NASM (Windows)
        if: matrix.platform == 'windows-latest'
        run: choco install nasm -y

      - name: Install NASM 2.15.05 (macOS)
        # NASM 2.16+ changed -MD flag behaviour: `nasm -MD /dev/null /dev/null`
        # now exits non-zero, breaking libaom-sys's cmake multipass test.
        # Build 2.15.05 from source so we get a native arm64 binary that passes the test.
        if: matrix.platform == 'macos-14'
        run: |
          curl -L https://www.nasm.us/pub/nasm/releasebuilds/2.15.05/nasm-2.15.05.tar.xz \
               -o /tmp/nasm.tar.xz
          tar -xf /tmp/nasm.tar.xz -C /tmp
          cd /tmp/nasm-2.15.05
          ./configure --prefix=/usr/local
          make -j$(sysctl -n hw.ncpu)
          sudo make install
          nasm --version

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install frontend deps
        run: npm ci

      - name: Build app (Tauri)
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'Hando ${{ github.ref_name }}'
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}

      - name: Ad-hoc sign macOS app
        # Zero-cost ad-hoc signature. Doesn't remove the Gatekeeper warning
        # (only Apple notarization does), but ensures Apple Silicon can launch
        # the binary at all and gives the app a stable code identity.
        if: matrix.platform == 'macos-14'
        run: |
          APP="src-tauri/target/universal-apple-darwin/release/bundle/macos/Hando.app"
          codesign --force --deep --sign - "$APP"
          codesign --verify --verbose=2 "$APP"

      - name: Build portable artifact
        env:
          TARGET: ${{ matrix.platform == 'macos-14' && 'universal-apple-darwin' || '' }}
        run: node scripts/build-dist.mjs

      - name: Upload portable artifact
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload ${{ github.ref_name }} dist-final/* --clobber
